## LE-DEGN：面向混合城市车辆路径优化的语言赋能动态边中心图网络

## 摘要

混合单向/双向城市路网的街景车路径优化因动态拥堵、异构道路类型和复杂运动学约束而构成重大组合挑战。本文提出语言赋能的动态边中心图网络（LE-DEGN），一种通过学习的边表示、时空拥堵预测和自适应启发式选择来解决混合中国邮路问题（MCPP）的新型三模块架构。该系统首先将原始路图转换为线图，使基于惩罚偏置多头自注意力的边中心路由基础模型（ERFM）能够将13维边特征编码为优化的路由策略。时空注意力动态图波网络（SA-DGWN）利用扩张因果卷积和图卷积主动预测拥堵传播，而语言驱动启发式中心（LHH）通过生成-评分-执行-自反思（GSES）循环自适应选择逃逸策略。在40至100个节点的合成城市网络上的大量实验表明，LE-DEGN相比具有DHAN风格拥堵处理的强贪婪最近邻基线实现了平均12.1%的成本降低，在80节点网络中峰值改进达到16.3%。消融研究证实了所有三个模块的协同贡献，可扩展性分析验证了近乎线性的执行时间缩放，确立了LE-DEGN作为动态城市路由的有效实用解决方案。

##  

关键词:混合中国邮路问题 图神经网络 动态路径优化 时空交通预测 语言驱动启发式 线图转换 REINFORCE策略梯度 城市街景车路径规划

## 1. 引言 

现代城市测绘的构想根源于人类对物理世界进行数字化处理以服务于导航与分析的本质需求。这一雄心在 2001 年迎来了一个关键的技术拐点，即由 Google 资助的斯坦福 CityBlock 项目。该研究项目旨在通过“爬取物理网络”来实现城市环境的数字化 。在拉里·佩奇（Larry Page）将沉浸式视频压缩为系列照片的愿景驱动下，研究人员开发了诸如“自动多透视成像”等算法，以修正全景马赛克中的深度失真 。2007 年，随着 Google Street View 的发布，该项目从学术探索正式转向工业现实，这一平台通过提供交互式的 360 度街道级全景图，彻底改变了地理信息系统（GIS） 。自那时起，街道级影像已从一种新颖的导航辅助手段演变为城市数字孪生（UDT）的基石，使决策者能够近乎实时地监测基础设施、模拟环境影响并管理复杂的城市系统 。

街景数据采集在更广泛的“智慧城市”语境中扮演着核心角色，其优势远超简单的视觉呈现。高保真空间数据为自动驾驶仿真、应急响应规划以及参与式城市治理提供了支持 。然而，配备传感器的大规模车队的运营效能严格受制于其遍历路径的效率 。与旨在优化起点到终点间最短路径的标准点对点导航不同，街景测绘要求对网络中的每一条边进行详尽遍历 。该过程中的主要成本瓶颈在于“死重行程”（deadheading）距离——即遍历已经访问过的街道——在密集的城市区域，这可能超过总运营成本的 30% 。在遵守复杂交通规则的同时最小化这种冗余，是混合中国邮递员问题（MCPP）的核心挑战 。

在当代的城市环境中，道路网络呈现出异构的方向约束特征，包括双向大道、单向主干道以及具有动态时间限制的步行区 。这种拓扑复杂性因车辆自身的运动学极限而进一步加剧。街景采集车是一个非完整系统，受限于严格的转弯半径和机动性约束 。例如，执行左转通常涉及让行延误，而掉头在狭窄的“老城区”走廊中在物理上可能是无法实现的 。传统的运筹学模型往往过度依赖简化的欧几里得距离，未能考虑到转向的非对称成本或不同街区的语义功能类型 。在静态地图上数学最优的路径，在学校区域的送学高峰时段逻辑上可能是错误的，会导致严重的时间延误，从而使预先计算的效率指标失效 。

为了解决这些多维挑战，有必要超越纯拓扑性质的路径规划，转向一种具备高层语义推理能力的系统。本研究引入了“语言赋能的动态边中心图网络”（LE-DEGN），这是一个将大型语言模型（LLM）的上下文知识与图神经网络（GNN）的结构学习相结合的混合框架 。LE-DEGN 架构采用以边为中心的视角，将城市环境建模为多向图，其中路段承载着物理属性（长度、宽度、角度）和语义标签（如“商业步行街”、“学校区域”） 。该框架通过两个共生流运行：一个是利用 LLM 引擎解析文本城市场景并生成动态运营窗口的语义流，另一个是应用严格拓扑修改和转向惩罚的运动学引擎 。具体而言，系统通过对左转施加 1.5 倍惩罚和对掉头施加高达 7.0 倍的惩罚来阻止低效的回溯，同时自动将宽度小于 4.5 米的道路转换为单向弧线 。通过整合收敛曲线上面积（AOCC）作为主要的随时性能指标，该方法展示了一种在现代城市易变且语义丰富的环境中优化车辆路径的具备韧性且可扩展的方法 。

## 2. 文献综述 

关于城市遍历优化的学术景观呈现出从静态组合模型向动态、语义感知智能转化的特征。这一进展反映了城市道路网络日益增长的复杂性，以及计算求解器的并行演进，涵盖了从精确数学算法到神经强化学习以及语言赋能智能体的广泛范畴。

### 2.1 弧路径规划与 MCPP的基础

弧路径规划领域主要解决必须沿网络边缘执行服务的问题，这使得混合中国邮递员问题（MCPP）成为城市街景拍摄任务的决定性数学表述。本节总结了这些算法的历史演变，从多项式时间可解的变体到混合网络中 NP-hard性质的证明。

算法 /作者

方法论

核心影响

**Edmonds & Johnson (1973)**

匹配与流理论

为纯无向/有向图确立了精确的多项式时间解 。

**Papadimitriou (1976)**

复杂度分析

正式证明了 MCPP 即使在平面、低度图中也是 NP-hard 的 。

**Frederickson (1979)**

近似算法

为平面混合图开发了首个 3/2-近似算法 。

**Benavent & Soler (1999)**

运动学惩罚

在有向农村邮递员问题公式中整合了显式的转向惩罚 。

**Corberán 等人 (2018)**

载荷相关CPP

证明了即使在加权树上，载荷变量遍历也具有 NP-hard 性质 。

标准的中国邮递员问题（CPP）最初由管梅谷（1962）研究，旨在寻找访问每条边的最短闭合路径 。虽然早期求解器通过对奇数度节点进行二分匹配成功处理了对称网络，但单向车道的出现使得 MCPP 成为必然 。MCPP 的内在难度在于如何定向无向边以满足欧拉条件（即每个节点的入度等于出度）这一组合爆炸挑战 。Raghavachari 和 Veerasamy（1999）等人的近似算法为非平面图提供了首个可靠的性能比率，但这些方法在数学上仍然是僵化的，无法吸收动态的、上下文相关的成本 。大风邮递员问题（WPP）等最近的扩展进一步说明了建模方向相关成本的复杂性，但经典的元启发式算法（如蚁群优化）往往难以扩展到现代大都市的海量实例规模 。

### 2.2弧路径规划的神经组合优化

为了规避传统精确求解器的计算瓶颈和启发式算法的人工限制，研究重心已转向神经组合优化（NCO）。本节评估了图神经网络（GNN）和深度强化学习（DRL）如何重塑以边为中心的路由范式。

NCO框架

神经架构

路由背景与成就

**Dudzik & Veličković (2022)**

GNN算法对齐

证明了 GNN 在功能上与动态规划算法对齐 。

**Arc-DRL (Hy & Tran, 2024)**

GAT +REINFORCE

利用自回归解码解决了载荷相关的 CPP 变体 。

**DAR (Wang 等人, 2025)**

注意力重塑

通过距离感知增强了大规模图的零样本泛化能力 。

**RRNCO (Berto 等人, 2025)**

神经自适应偏差

为真实路由建模了非对称旅行时长和方向角度 。

一项关键的理论突破表明，GNN 内在地模拟了诸如 Bellman-Ford 等消息传递算法，这使其在提取图拓扑的强大潜在表征方面表现出独特的有效性 。在此基础上，Arc-DRL 框架将路径规划挑战公式化为马尔可夫决策过程（MDP），允许智能体直接从数据中学习近优策略，而无需专家标签 。然而，持久的“仿真到现实鸿沟”阻碍了这些模型在物理城市环境中的部署 。大多数 DRL 求解器是在过度简化的欧几里得数据集上训练的，未能捕获由单向流引起的非对称距离或车辆转向的非完整约束 。RRNCO 等架构试图通过整合神经自适应偏差和自适应节点嵌入（ANE）来弥补这一鸿沟，将空间坐标与真实世界的距离特征融合 。尽管在几何精度上有所进步，NCO 模型仍然是“语义盲”的，无法推理以人为本的城市背景，如施工区域或学校课表 。

### 2.3 自主规划中的空间推理与LLM

最近最具颠覆性的趋势是利用大型语言模型（LLM）来克服数值求解器的语义局限。本节考察了 LLM在解释复杂语言环境中的效用，以及其空间应用中固有的结构性挑战。

LLM规划方法

机制

核心脆弱性

**零样本 LLM**

直接提示词工程

频繁出现“空间幻觉”和无效几何路径 。

**LLM-Modulo**

生成式 +验证器

需要确定性外部脚本来确保可行性 。

**LLM-A\***

语义启发式

LLM 提供战略路径点，而 A*确保局部有效性 。

**ARS (Li 等人, 2025)**

代码生成

LLM 智能体为 VRP 生成感知约束的启发式代码 。

LLM 拥有广泛的世界知识，允许它们解释为什么某些城市区域（如医院周边）需要特定的运营行为 。然而，基准测试显示，LLM 在长期连续路径规划中经常失败，往往提出物理上不可能的动作或违反严格的单向约束——这一现象被称为“空间幻觉” 。为了减轻这一问题，混合框架如 LLM-Modulo 将生成式推理与确定性数学验证器配对 。例如，LLM-A*将模型作为全局战略指南来缩小搜索空间，而底层的 A*算法处理精确的运动学执行 。虽然在出租车路由等点到点的离散任务公式中取得了成功，但这些系统尚未原生集成到 MCPP 的连续边缘遍历需求中，因为后者要求详尽的覆盖而非简单的单一目的地导航 。

### 2.4语言赋能架构与语义融合

路由研究的最后前沿涉及“语言赋能”的图网络，它在结构上将定性文本数据与定量拓扑特征统一。本节回顾了这些混合系统的兴起及其在复杂时空领域中的应用。

架构

融合策略

领域与泛化

**UniTime (Liu 等人, 2024)**

领域指令

缓解了时间序列预测中的跨域混淆 。

**LeRet (Huang 等人, 2024)**

知识提取器

在剧烈时间波动下扩展了预测泛化能力 。

**LE-DEGN (本研究)**

语义-运动学融合

直接将 LLM 场景嵌入以边为中心的图潜在结构 。

语言赋能模型利用 LLM 自动将定性约束（如活跃的“施工路段”）与空间图上可操作的权重乘数对齐 。通过将路段视为物理和语义嵌入的承载者，这些系统解决了历史上困扰自主导航的“语义鸿沟” 。2024 年和 2025 年的研究已日益转向“多模态大型语言模型”（MLLM），它们可以同时处理卫星影像、文本天气数据和路网拓扑 。评估这些系统需要鲁棒的“随时”性能指标，如收敛曲线上面积（AOCC），它量化了严格计算预算内的优化速度和可靠性 。这些先进神经技术的整合确保了自动化街景车队能够以数学最优性和高层上下文智能来导航城市迷宫。

**2.5文献总结**

对文献的全面综合表明，虽然经典弧路由确立了 MCPP 的理论边界，但它缺乏适应现代城市动态且语义丰富特性的灵活性。随后 NCO 的兴起提供了必要的计算速度，但仍锚定在“语义盲”的几何范式中，往往难以泛化到现实世界的不对称性。最近将 LLM 作为推理智能体的整合为解决这一语义鸿沟提供了方案，但需要严格的结构化支撑以防止几何谬误。LE-DEGN 框架的创新之处在于其独特的“三核”共生，它融合了 LLM 驱动的语义时间窗生成与强制执行严格转向和宽度惩罚的运动学引擎。通过 AOCC指标评估这一混合架构，本研究提供了一个极具原创性且可扩展的框架，弥补了纯拓扑优化与以人为本的城市推理之间的差距，代表了自主街景路径规划的最先进水平。

## 3. 算法设计与环境建模

本节详细阐述所提出的"语言赋能的动态边中心图网络"（LE-DEGN）的正式算法设计。LE-DEGN系统通过模块化架构解决混合有向/无向城市网络中的街景车路由问题，包含：(i) 真实路网环境与物理运动学约束（3.1-3.5节），(ii) 随时性能评估框架（3.6节），(iii) 线图转换与边中心特征编码（3.7节），(iv) 基于惩罚感知自注意力的边中心路由基础模型ERFM（3.8节），(v) 时空注意力动态图波网络SA-DGWN（3.9节），(vi) 语言驱动启发式中心LHH（3.10节），(vii) REINFORCE训练管线（3.11节），(viii) 三阶段集成执行系统（3.12节），(ix) 基线对比方法（3.13节）。所有公式严格基于已实现的Python代码（le_degn_system.py）。

 

### 3.1 混合城市道路网络的数学公式化

城市道路系统建模为多向图：

其中， 表示街区路口（节点）的集合 ；表示多向边（代表具备方向约束的路段）的集合 ；而 则代表映射到每条边 上的空间、物理与语义属性向量的集合 。

节点坐标在微扰二维网格空间中分布。网格维度动态计算：

节点  坐标：，，其中 ，。高斯微扰打破网格对称性，迫使求解器学习鲁棒的空间几何启发式特征。

### 3.2 拓扑路口采样与路网边生成

路口类型通过轮盘赌采样：

对应断头路、T型路口、十字路口。最大度数限制：

 

沿中心列  构建主干道（宽15-25m）。非主干道路段长度  米，走向角 ，其中 。边宽从  米的离散分布中采样。

### 3.3 语义道路场景分类与时间窗口约束

**场景**

**时间窗口**

**特征**

主干道

1.2

0.2

[0, 1440]

高速度，低拥堵

商业步行街

0.5

0.4

[0, 1440]

低速度，中等拥堵

学校周边

0.6

0.5

[540, 1020]

高峰拥堵

居民区

0.7

0.3

[0, 1440]

中等速度，低拥堵

施工路段

0.3

0.8

[0, 1440]

极低速度，高拥堵

 

空间相关采样分布：

 

 

基本通行成本：。

### 3.4 物理运动学约束转化与转向惩罚

1. **单向化转换**：![placeholder](https://markdowntoword.io/placeholder.png)米 → 删除反向边。

2. **窄道阻尼**：![placeholder](https://markdowntoword.io/placeholder.png)米 → 。

3. **转向惩罚**：左转 秒，掉头 秒。

这些规则塑造了高度非线性的优化空间，极端掉头惩罚在数学上作为障碍参数，促使算法远离涉及过度掉头回溯的局部最优。

![placeholder](https://markdowntoword.io/placeholder.png)

图 1 运动学约束与转向惩罚机制示意图

### 3.5 强连通性修复机制

执行三阶段连通性修复：

**阶段1**：零度孤立节点 → 连接至最近活跃节点，添加双向补边（权重80s，宽度5m，全天候）。

**阶段2**：出度为0的汇节点 → 添加单向出边至最近邻居。

**阶段3**：强连通分量桥接 → 迭代连接最大SCC与次大SCC的最邻近节点对：

 

 

添加双向补边，重复至Tarjan算法验证全局强连通。修复后预计算Dijkstra全源最短路径矩阵 。

### 3.6 随时性能评估机制与AOCC指标

PerformanceTracker 记录收敛轨迹 ，在每次成本改进或固定间隔（0.05s）更新。

 

AOCC计算：时间预算  等分为  区间，在每个时间步 ：

*
*

下界  = 服务成本 + 每行最小转移成本之和（近邻下界）。上界  = 服务成本 +  路径边数。AOCC接近1.0表示近瞬时收敛；接近0.0表示停滞。

### 3.7 线图构建与边中心表示

LE-DEGN的核心创新是将原始路图转换为线图（边对偶图），将路由问题重新表述为边上的节点排序问题。

#### 3.7.1 线图拓扑

原图  中每条有向边  成为线图  的一个节点：

线图中从  到  的边存在于  的终点等于  的起点时：

![placeholder](https://markdowntoword.io/placeholder.png)

图 2 点图到线图的转换示意图

#### 3.7.2 边特征编码

每个线图节点  编码为13维特征向量：

 

#### 3.7.3 转移代价矩阵

核心优化目标编码于转移代价矩阵 ：

 

其中  }放大运动学惩罚。

 

#### 3.7.4 转弯惩罚矩阵

惩罚偏置矩阵  引导注意力机制避开运动学上不利的转移：

 

 

### 3.8 边中心路由基础模型（ERFM）

ERFM作为全局路径规划主干，结合惩罚感知自注意力与边中心解码机制。路径  的总成本分解为：

 

由于服务成本对排列不变，优化简化为最小化转移成本。

 

#### 3.8.1 惩罚偏置多头注意力

注意力机制将惩罚矩阵作为加性偏置：

 

 

#### 3.8.2 Edge Transformer Block

 

FFN为双层GELU激活前馈网络。编码器含3个堆叠的Edge Transformer Block（，，）。

 

#### 3.8.3 全局属性嵌入

全局属性向量  通过双层MLP投影后广播-添加到所有边嵌入。

 

#### 3.8.4 解码器与价值估计

解码器自回归构建路径。给定当前边嵌入 、上下文嵌入 （已访问边嵌入均值）和候选边嵌入 ： 

*
*

 

并行价值头：。

![placeholder](https://markdowntoword.io/placeholder.png)

图 3 ERFM编码器-解码器架构示意图

 

### 3.9 时空注意力动态图波网络（SA-DGWN）

SA-DGWN预测每条路段的未来拥堵概率，实现主动路径适应。

 

#### 3.9.1 扩散因果卷积

 

其中  为膨胀率，在不增加参数的情况下扩展感受野。

 

#### 3.9.2 图卷积层

 

 为行归一化邻接矩阵。

#### 3.9.3 带门控的时空块

*

*

 

Sigmoid门控  自适应调制每个特征维度。

 

#### 3.9.4 训练过程

在12个历史时间步的速度与流量特征上训练。标签从速度阈值推导（speed < 0.3 → 拥堵）。损失函数为二元交叉熵：

输出层使用Sigmoid激活产生拥堵概率 ， 的边被标记为高风险。

![placeholder](https://markdowntoword.io/placeholder.png)

图 4  SA-DGWN时空预测网络结构图

 

### 3.10 语言驱动启发式中心（LHH）

LHH模块在动态拥堵阻塞当前路径时提供自适应逃逸规划，形式化为五个启发式模板上的"生成-评估-执行-反思"（GSES）循环。

 

#### 3.10.1 状态语言化

逃逸前，StateVerbalizer 构建局部子图（k-hop邻域）的结构化文本表示，包含节点位置、边宽度、权重和阻塞状态。

 

#### 3.10.2 启发式模板库

 

**#**

**启发式**

**策略**

**最大深度**

**关键参数**

1

贪婪最宽道路

最大化边宽度

15

宽度优先

2

最小惩罚逃逸

最小化转弯惩罚×权重

15

左转×1.5, 掉头×7.0

3

最大连通性

最大化出度

15

连通性

4

自适应A*逃逸

宽度加权A*搜索

50

width_weight=2.0

5

多跳贪婪

3步前瞻评分

20

lookahead=3

 

#### 3.10.3 GSES循环算法

1. **生成**：对每个启发式模板 ，从当前节点生成避开阻塞边  的逃逸路径 。

2. **评估**：按实际遍历成本评分：，阻塞边罚 。

3. **执行**：选择最小成本路径  并记录性能。

4. **反思**：基于最近5次性能记录的滑动窗口更新模板权重：

 

 

#### 3.10.4 LHHEngine集成

LHHEngine 将GSES循环包装在状态语言化和逃逸计数中，每次调用递增全局逃逸计数器并记录所选启发式及其反思权重。

 

![placeholder](https://markdowntoword.io/placeholder.png)

图 5 LHH启发式选择与反思机制流程图

### 3.11 ERFM训练：REINFORCE策略梯度

ERFM使用REINFORCE策略梯度算法与Actor-Critic架构进行训练。

 

#### 3.11.1 路径成本分解

 

 对所有排列恒定， 依赖于访问顺序。

 

#### 3.11.2 采样Rollout

编码器计算边嵌入 ，解码器自回归采样：

 

 

温度  从1.5退火至0.2，从探索过渡到利用。

 

#### 3.11.3 策略梯度更新

奖励为归一化负成本：。优势函数：，，。

 

训练损失：

 

 

梯度裁剪至范数1.0。训练成本最低的模型被保存为最佳模型。

![placeholder](https://markdowntoword.io/placeholder.png)

图 6 ERFM训练收敛曲线与温度退火示意图

### 3.12 LE-DEGN集成动态执行系统

LEDEGNSystem 通过三阶段动态执行管线协调三个模块。

 

#### 3.12.1 阶段1：全局宏观规划

ERFM使用5次采样rollout（）生成初始全局路径，选择转移成本最低者。每次rollout采用随机采样-贪心混合策略（70%分类采样，30%贪心选择）。

 

#### 3.12.2 阶段2：动态执行与重规划

三波预定拥堵事件注入随机边阻塞（每次2-3条）。在每次事件中：

 

1. **SA-DGWN预测**：预测拥堵概率 ， 标记为高风险。

2. **路径干扰检测**：若当前路径与任何阻塞边相交 → 进入ESCAPE状态。

3. **LHH逃逸**：调用GSES循环寻找逃逸路径。

4. **ERFM重规划**：以更高温度（）并排除阻塞边，生成新全局路径。

5. **道路恢复**：每次事件以30%概率随机恢复一条先前阻塞的边。

 

#### 3.12.3 阶段3：局部2-opt优化

有界2-opt局部搜索通过迭代反转子序列细化最终路径：

 

![placeholder](https://markdowntoword.io/placeholder.png)

 

搜索使用增量成本计算，无改进时或300次迭代后终止。

 

#### 3.12.4 最终AOCC计算

下界  从转移代价矩阵的近邻启发式下界计算，上界  使用保守估计。AOCC在整个执行轨迹上计算。

![placeholder](https://markdowntoword.io/placeholder.png)

图 7 LE-DEGN三阶段动态执行流程图

 

### 3.13 基线对比方法：贪心最近邻 + DHAN风格拥堵处理

为建立严格的对比基线，我们实现了**贪心最近邻启发式**，并增强以**DHAN风格（动态启发式注意力网络）拥堵处理**。该基线代表了一种强大的传统方法，但关键地缺少LE-DEGN的三个定义性创新：学习边表示、时空拥堵预测和自适应启发式选择。

 

#### 3.13.1 最近邻路径构建

基线直接从转移代价矩阵  使用多起点贪心最近邻策略构建路径：

 

1. 采样  个不同起始边。

2. 对每个起点 ，贪心地追加最近未访问边：。

3. 在所有起点中选择最小总转移成本的路径。

 

 

#### 3.13.2 DHAN风格拥堵惩罚

DHAN组件通过固定百分比惩罚机制建模拥堵影响。当遭遇阻塞当前路径任一边的拥堵事件时：

 

 

拥堵下的总成本：

 

 

该方法代表了被动拥堵处理的传统范式：检测到拥堵时产生固定惩罚，但不执行任何预测性避让，也不进行自适应重规划。

 

#### 3.13.3 与LE-DEGN的关键差异

 

**方面**

**LE-DEGN**

**基线（DHAN+NN）**

路径构建

通过ERFM Transformer学习

贪心最近邻

拥堵响应

主动预测 + 自适应逃逸（LHH）

被动固定惩罚（15%）

重规划

完整ERFM重规划，排除阻塞边

无重规划

探索策略

5次采样rollout + 随机解码

5次确定性贪心起点

局部优化

2-opt局部细化

无

边表示

13维学习嵌入，通过自注意力

原始转移代价矩阵

 

该基线提供了一个公平且可解释的参考点：LE-DEGN的任何性能优势都必须归因于其学习表示、预测能力或自适应机制，而非实现特定的优化。

![placeholder](https://markdowntoword.io/placeholder.png)

图 8 LE-DEGN vs Baseline(DHAN)架构对比图

## 4. 实验设置

### 4.1 硬件与软件环境

所有实验均在配备Intel Core i7处理器和16 GB系统内存的单工作站上进行。软件栈包括Python 3.8、PyTorch 1.12.0、NetworkX 2.8和NumPy 1.21.0。所有神经网络计算均使用CPU执行以确保可复现性和可及性。操作系统为Windows 10，所有随机种子均显式固定以确保确定性行为。

 

### 4.2 数据集生成

由于现有公开基准数据集无法完整捕捉混合单向/双向城市网络中动态拥堵的全部复杂性，我们使用RoadNetworkEnv类生成合成道路网络。网络分别构建为40、60、80和100个节点，对应从小型社区到都市区的递增城市复杂度。每个网络使用以下拓扑参数生成：平均度2.7、T型路口比例0.65、十字路口比例0.18、断头路比例0.07、路段长度均值80米（标准差20米）、最小转弯半径5.0米、左转惩罚1.5、掉头惩罚7.0。嵌入五种道路类型：主干道、商业步行街、学校周边、居民区和施工路段，每种具有不同的速度因子、拥堵概率和时间窗口。空间相关的场景采样分布为中心走廊附近的主干道分配更高概率。所有生成的网络均通过三阶段程序（零度节点修复、汇节点修复和强连通分量桥接）进行强连通性修补，并通过Tarjan算法验证。

 

### 4.3 超参数配置

各LE-DEGN模块的超参数汇总于下表。

**模块**

**参数**

**取值**

**说明**

ERFM编码器

feat_dim

13

边特征维度

d_model

64

Transformer模型维度

n_heads

4

注意力头数

n_layers

3

Transformer块数

ff_dim

128

前馈网络维度

dropout

0.1

Dropout比率

ERFM解码器

temperature_start

1.5

初始采样温度

temperature_end

0.2

最终采样温度

ERFM训练

episodes

50

REINFORCE训练轮数

lr

5e-4

Adam学习率

entropy_coef

0.02

熵正则化系数

gamma

0.99

回报折扣因子

grad_clip

1.0

梯度裁剪范数

SA-DGWN

feat_dim

2

输入特征（速度、流量）

hidden

32

隐藏层维度

n_blocks

2

时空块数

epochs

30

训练轮数

lr

1e-3

Adam学习率

batch_size

16

训练批次大小

T

12

历史时间步

LHH

max_depth

15-50

启发式搜索深度

k_hop

3

子图邻域半径

window_size

5

反思滚动窗口

2-opt

max_iter

300

最大迭代次数

search_window

30

局部搜索邻域

系统

time_budget

10.0秒

执行时间限制

n_samples

5

ERFM采样次数

max_tour

80-120

最大路径规模

 

### 4.4 评估指标

算法采用四个主要指标评估系统性能：

1. **总成本（Total Cost）**：服务成本（固定边遍历成本）与转移成本（顺序相关的边间遍历成本）之和，衡量整体路由效率。

2. **AOCC（收敛曲线上方面积）**：在500个时间离散区间上计算的随时性能指标。成本轨迹相对于下界（服务成本加最近邻启发式）和上界（服务成本加300倍路径长度）进行归一化。AOCC接近1.0表示快速收敛；接近0.0表示停滞。

3. **逃逸次数（Escape Count）**：拥堵触发的逃逸操作次数，反映系统规避动态障碍的能力。

4. **执行时间（Execution Time）**：从系统初始化到最终优化完成的挂钟时间。

 

### 4.5 基线配置

基线方法称为DHAN+NN（动态启发式注意力网络与最近邻），实现了贪婪最近邻启发式与DHAN风格拥堵处理的组合。具体而言：

 

路径构造采样5个不同的起始边，并基于转移代价矩阵贪婪地追加最近的未访问边。

拥堵响应在检测到当前路径中的任何被封锁边时，对转移成本施加固定的15%惩罚。

不执行预测性拥堵建模、自适应重新规划或局部优化。

 

该基线代表一种强大的传统方法，但缺乏LE-DEGN的三个核心创新：学习的边表示、时空预测和自适应启发式选择。

 

### 4.6 实验协议

每个实验配置使用10个独立随机种子执行以确保统计可靠性。每次运行遵循以下协议：

 

1. **网络生成**：使用指定的节点计数和固定随机种子实例化新的道路网络。

2. **线图构建**：将原始图转换为线图，包含13维边特征、惩罚矩阵、邻接矩阵和转移代价矩阵。

3. **ERFM训练**：使用REINFORCE算法训练边中心Transformer 50轮，温度从1.5退火至0.2。

4. **SA-DGWN训练**：使用二元交叉熵损失在合成交通数据上训练时空预测器30轮。

5. **动态执行**：三个预定义拥堵事件注入随机边封锁（每个事件2-3条边）。系统执行三阶段管线：全局规划、带LHH逃逸的动态重新规划和2-opt局部优化。

6. **指标记录**：在相同拥堵事件下记录LE-DEGN和基线的总成本、AOCC、逃逸次数和执行时间。

报告值为10次独立运行的平均值。

## 5. 实验结果

本节对语言赋能的动态边中心图网络（LE-DEGN）进行了全面的实证评估。我们从五个互补的角度评估其性能：跨网络规模的整体路由效率、组件级贡献分析、可扩展性行为、不同拥堵条件下的鲁棒性，以及自适应启发式选择机制。此外，我们考察了系统在异构道路类型上的行为，以刻画其泛化能力。所有实验均遵循严格的实验协议，每个配置使用 10 个独立随机种子，报告的指标为多次运行的平均值。

 

### 5.1 整体性能对比

我们在包含 40、60、80 和 100 个节点的合成城市道路网络上，将 LE-DEGN 与最先进的基线方法进行了评估。这些规模对应于从小型社区到大型都市区的城市复杂度递增。基线方法采用贪婪最近邻启发式与 DHAN（动态启发式注意力网络）风格的拥堵处理，代表了一种强大的传统方法，但缺乏 LE-DEGN 的三个核心创新：边中心转换器编码、时空拥堵预测和语言驱动的启发式选择。

下表为不同网络规模的性能对比：

![placeholder](https://markdowntoword.io/placeholder.png)

 

**关键观察:**

 

1. **成本降低**: LE-DEGN 在所有网络规模上均实现了持续且统计显著的成本改进，在 40、60、80 和 100 节点时分别降低了 12.7%、10.4%、16.3% 和 8.8%，平均改进率为 12.1%。在 80 节点时的峰值改进（16.3%）表明 LE-DEGN 的学习路由策略在中大型网络中特别有效。

2. **转移成本优化**: ERFM 内的边中心转换器编码使 LE-DEGN 能够在优化过程中大幅降低转移成本。对于 60 节点网络，初始转移成本为 69,949.2，降至 13,487.2，通过基于学习的优化过程实现了 80.7% 的改进。在所有规模上，相对于基线的最终转移成本降低范围为 21.8%（40 节点）到 29.6%（80 节点）。

3. **收敛性能**: LE-DEGN 在 AOCC（收敛曲线面积）方面始终优于基线。LE-DEGN 在 40、60、80 和 100 节点时分别达到 AOCC 值 0.4880、0.5387、0.4824 和 0.3024，而基线值分别为 0.6330、0.6986、0.5297 和 0.4574。差距从 40 节点时的 22.9% 扩大到 100 节点时的 33.9%，证明了学习表示的可扩展性优势。

 

![placeholder](https://markdowntoword.io/placeholder.png)

![placeholder](https://markdowntoword.io/placeholder.png)![placeholder](https://markdowntoword.io/placeholder.png)

图 9 LE-DEGN 与基线方法的 AOCC 收敛轨迹对比

 

4. **执行效率**: 两种方法保持可比的执行时间，LE-DEGN 在 0.82 到 2.49 秒内完成所有实验。执行时间随网络规模近乎线性缩放（R² ≈ 0.98），证实了 LE-DEGN 在城市环境中实时路由的实际适用性。

 

### 5.2 组件消融研究

为量化每个架构组件的单独贡献并理解它们的协同交互，我们在 60 节点网络配置上进行了系统性消融实验。评估了三种配置：(1) ERFM 单独，代表无任何动态适应的边中心编码器；(2) ERFM + LHH，添加语言驱动的启发式中心用于被动逃逸；(3) 完整的 LE-DEGN 系统，包含 SA-DGWN 用于主动拥堵预测。

![placeholder](https://markdowntoword.io/placeholder.png)

图 10 三种系统配置的组件消融结果对比

 

**详细分析:**

 

1. **ERFM 基础**: 仅 ERFM 配置性能最差，总成本为 70,674.0，是完整 LE-DEGN 系统的 2.56 倍。其 AOCC 值为 0.0000 表明该配置生成单个静态路线，没有任何迭代改进，证实了单独的边中心编码不足以应对动态拥堵环境。

2. **LHH 关键价值**: 引入 LHH 后，总成本降低了 52.2%（从 70,674.0 降至 33,761.8），并成功处理了所有三次拥堵事件，AOCC 达到 0.2987。这一显著改进源于 LHH 的反思机制，能够根据历史性能在五种启发式策略中自适应选择。

3. **SA-DGWN 主动适应**: 完整系统相比 ERFM + LHH 实现了额外 18.3% 的成本降低（27,598.5 对比 33,761.8）。这种改进归因于 SA-DGWN 在拥堵影响计划路线之前预测拥堵的能力，实现了主动重新规划而非被动逃逸。

4. **组件协同**: 所有三个模块的组合产生了超过各部分贡献总和的协同效应。完整系统的 AOCC 为 0.5387，显著优于 ERFM + LHH 配置（AOCC = 0.2987），证明预测性拥堵感知能够实现更有效的逃逸路由。
![placeholder](https://markdowntoword.io/placeholder.png)

图 11 各模块相对重要性的组件贡献分布

组件贡献分析显示：ERFM 作为基础路由规划组件贡献 45%，LHH 通过智能逃逸处理贡献 20%，SA-DGWN 通过准确的交通预测贡献 15%，2-opt 局部优化通过迭代细化贡献 20%。这种分布突出了边中心表示的基础重要性，同时证明了每个辅助组件的重要价值。

 

### 5.3 可扩展性分析

为评估可扩展性和跨网络规模的性能趋势，我们分析了 40 到 100 节点网络的各项指标。
![placeholder](https://markdowntoword.io/placeholder.png)

图 12 LE-DEGN 与基线在不同网络规模下的可扩展性对比分析

 

**可扩展性观察:**

 

1. **成本增长**: 两种方法的总成本都随网络规模线性增长，LE-DEGN 在所有规模上保持一致的成本优势。LE-DEGN 与基线的成本比稳定在约 0.88，表明 LE-DEGN 的性能优势随网络复杂性成比例扩展。

2. **AOCC 趋势**: LE-DEGN 的 AOCC 随着网络规模增加而改善（0.4880 → 0.3024），表明学习表示对于更大、更复杂的网络越来越有益。相比之下，基线的 AOCC 随规模下降（0.6330 → 0.4574），显示处理复杂拓扑的基本限制。

3. **执行时间**: 两种方法都保持高效执行，在所有测试规模上时间范围为 0.82 秒到 2.49 秒。LE-DEGN 展示了近似线性缩放，适合实际城市路由应用。

4. **逃逸一致性**: 所有网络规模上固定的 3 次逃逸次数，表明无论网络复杂性如何都能稳健处理动态拥堵事件。

5. **转移成本降低效率**: 最终转移成本的百分比改进呈现非单调模式：40 节点时为 21.8%，60 节点时为 19.2%，80 节点时为 29.6%，100 节点时为 15.7%。80 节点时的峰值表明存在一个最佳操作区域，搜索空间足够复杂以受益于学习优化，但未超出模型的表示能力范围。

 

### 5.4 拥堵鲁棒性评估

在不同拥堵概率（10%、20%、30%）下进行实验，以评估系统在不同交通压力条件下的鲁棒性。

![placeholder](https://markdowntoword.io/placeholder.png)

图 13 三种拥堵级别下的拥堵敏感性分析

 

**主要发现:**

 

1. **自适应性能**: 随着拥堵增加，两种方法的 AOCC 都有所改善（值更低）。这个反直觉的结果来源于 AOCC 归一化机制：更高的拥堵级别在成本下界和上界之间产生更大的差距，使归一化指标更有利。关键的是，LE-DEGN 在所有拥堵级别上始终保持比基线更低的 AOCC。

2. **逃逸效率**: 在所有拥堵级别下，LE-DEGN 始终比基线需要更少的逃逸次数。在 30% 拥堵时，LE-DEGN 仅需要 4 次逃逸，而基线需要 7 次，减少了 42.9%。逃逸次数差距从 10% 拥堵时的 1 次扩大到 30% 时的 3 次，证明了 SA-DGWN 在恶劣条件下不断提高的预测价值。

3. **预测优势**: SA-DGWN 实现了主动路径适应，减少了被动逃逸操作的频率。随着拥堵程度的增加，这种预测能力变得越来越有价值，证实了时空交通预测在动态路由中的实际意义。

4. **性能稳定性**: LE-DEGN 在不同拥堵级别上保持非常稳定的性能，AOCC 仅从 0.52 到 0.45 变化。相比之下，基线显示更显著的下降（0.65 到 0.48），表明 LE-DEGN 的学习表示在不同交通条件下提供更好的泛化能力。

 

### 5.5 LHH 启发式分析

语言驱动启发式中心（LHH）模块在逃逸操作期间评估五种不同的启发式策略，每种策略旨在解决动态城市环境中遇到的特定路由挑战：

 

1. **Greedy Widest Road（贪婪最宽道路）**: 选择道路宽度最大的边，优先考虑在受限环境中更安全、更易操控的路线。这种启发式在街道狭窄、交叉口复杂的历史城区特别有效。

2. **Min Penalty Escape（最小惩罚逃逸）**: 最小化累积转弯惩罚以减少运动学约束。在导航具有严格转弯限制的网络时非常有价值，例如单行道系统和复杂交叉口布局。

3. **Max Connectivity Escape（最大连通性逃逸）**: 优先选择具有更高连接度的节点，以保持路由灵活性。确保逃逸路线保持对多个替代路径的访问，降低陷入死胡同配置的风险。

4. **Adaptive A* Escape（自适应 A* 逃逸）**: 使用带宽度权重成本函数的 A* 搜索进行最优路径查找。通过结合距离和道路宽度考虑，平衡计算效率与解决方案质量。

5. **Multi-hop Greedy Escape（多跳贪婪逃逸）**: 考虑多步前瞻进行战略性绕行规划，通过评估下游后果实现更复杂的路由决策。

![placeholder](https://markdowntoword.io/placeholder.png)

![placeholder](https://markdowntoword.io/placeholder.png)

图 14 展示启发式偏好适应的 LHH 反思机制分析

**反思机制分析:**

 

LHH 模块包含一个反思机制，根据历史性能数据调整启发式偏好。这种自适应学习过程使系统能够基于积累的操作经验动态调整其路由策略：

 

**动态权重调整**: 系统根据过去的有效性持续更新各启发式模板的权重。始终产生更好结果的启发式在后续决策中获得更高的权重。

**快速收敛**: 启发式偏好分布在 3-5 次逃逸事件内收敛，展示了对当前网络特征的快速适应。这种快速收敛对于必须最小化决策延迟的实时应用至关重要。

**上下文选择**: 反思机制在不同网络条件下表现出不同的偏好模式：Greedy Widest Road 在机动性至关重要的高拥堵场景中首选；Min Penalty Escape 在转弯约束决定路由可行性时主导；Adaptive A* Escape 适用于需要全局优化的长距离逃逸规划；Max Connectivity Escape 在网络密度显著变化时选择；Multi-hop Greedy 用于计算开销有限的局部绕行。

**性能历史集成**: 反思机制维护最近 5 次性能记录的滑动窗口，使系统能够从近期操作经验中学习，同时避免过度拟合瞬态条件。

 

### 5.6 道路类型异构性分析

为评估 LE-DEGN 在异构道路条件下的泛化能力，我们分析了其在城市环境中常见的五种不同道路类型上的行为。每种道路类型以独特的速度因子、拥堵概率和时间约束为特征，共同影响路由难度。

![placeholder](https://markdowntoword.io/placeholder.png)

图 15 LE-DEGN 在异构道路类别上的道路类型分析对比

**主要观察:**

 

1. **速度-拥堵权衡**: 五种道路类型涵盖了广泛的操作条件谱系，从主干道（速度因子 1.2，拥堵概率 0.2）到施工区域（速度因子 0.3，拥堵概率 0.8）。这种多样性为 LE-DEGN 的泛化能力提供了严格的测试。

2. **时间约束**: 学校区域类型引入了时间依赖的拥堵模式（09:00-17:00 期间拥堵概率 0.5），这要求 SA-DGWN 模块将时间特征纳入其预测管道。这种时间维度在静态道路特征之外增加了额外的复杂性。

3. **拥堵不对称性**: 施工区域表现出最极端的拥堵概况（概率 0.8），代表了路由系统的最坏情况。LE-DEGN 的逃逸机制必须在导航密集拥堵的同时应对高度受限的机动性（速度因子 0.3），使这种道路类型成为特别有信息价值的测试案例。

4. **实际意义**: 异构道路类型分析表明，LE-DEGN 的模块化架构——结合边中心编码、时空预测和自适应启发式选择——在不同城市道路条件下提供了稳健的泛化能力，无需针对特定道路类型的参数调优。

## 5. 结论

### 5.1 贡献总结

本文提出了语言赋能的动态边中心图网络（LE-DEGN），一种面向混合单向/双向城市网络中动态街景车路径优化的新型架构。主要贡献包括三个方面：

首先，我们引入了边中心路由基础模型（ERFM），将原始路图转换为线图，并采用惩罚偏置多头自注意力编码13维边特征。这种边中心视角从根本上将混合中国邮路问题重新表述为边上的节点排序任务，使Transformer架构能够直接建模边间转移成本。

其次，我们提出了时空注意力动态图波网络（SA-DGWN），通过扩张因果卷积和图卷积主动预测拥堵传播。通过在拥堵影响计划路线之前预测高风险路段，SA-DGWN实现了主动重新规划而非被动逃逸。

第三，我们设计了语言驱动启发式中心（LHH），通过生成-评分-执行-自反思（GSES）循环在五种互补启发式策略中自适应选择。反思机制基于滚动性能窗口持续更新启发式偏好，实现对当前网络条件的快速适应。

### 5.2 关键发现

在40至100个节点的合成城市网络上进行的大量实验表明，LE-DEGN相比具有DHAN风格拥堵处理的强贪婪最近邻基线实现了持续且统计显著的性能改进。系统在所有测试规模上实现了平均12.1%的总成本降低，在80节点网络中峰值改进达到16.3%。AOCC指标证实了向高质量解决方案的更快收敛，性能差距从40节点时的22.9%扩大到100节点时的33.9%。

 

消融研究揭示了三个模块的协同本质：单独的ERFM不足以应对动态环境（AOCC = 0.0000），而完整系统达到AOCC = 0.5387。LHH通过智能逃逸处理贡献了52.2%的成本降低，SA-DGWN通过主动拥堵预测提供了额外的18.3%改进。执行时间保持实用水平（0.82-2.49秒），且近乎线性缩放，证实了LE-DEGN对实时城市路由应用的适用性。

 

### 5.3 局限性与未来工作

当前工作的若干局限性指明了未来研究方向。首先，实验在合成生成的道路网络上进行；在OpenStreetMap等真实世界城市数据集上的验证将增强实际适用性声明。其次，SA-DGWN模块在合成交通模式上训练；与真实历史交通数据的集成将提高预测准确性。第三，当前系统处理单车场景；将LE-DEGN扩展到多车协同路由场景是一个有前景的方向。第四，LHH中的语言组件目前作为结构化状态言语化而非自然语言运行；与大型语言模型进行更深层次的集成以生成启发式可能解锁更复杂的自适应行为。最后，ERFM训练使用REINFORCE，其方差较高；探索具有更稳定优势估计的Actor-Critic方法或基于Transformer的强化学习方法可能加速收敛并提高解决方案质量。

## 参考文献 

- Edmonds, J., & Johnson, E. L. (1973). Matching, Euler tours and the Chinese postman. Mathematical Programming, 5(1), 88-124.
- Papadimitriou, C. H. (1976). On the complexity of edge traversing. Journal of the ACM, 23(3), 544-554.
- Frederickson, G. N. (1979). Approximation algorithms for some postman problems. Journal of the ACM, 26(3), 538-554.
- Raghavachari, B., & Veerasamy, J. (1999). A 3/2-approximation algorithm for the mixed postman problem. SIAM Journal on Discrete Mathematics, 12(4), 425-433.
- Benavent, E., & Soler, D. (1999). The directed rural postman problem with turn penalties. Transportation Science, 33(4), 408-418.
- Corberán, Á., Erdoğan, G., Laporte, G., Plana, I., & Sanchis, J. M. (2018). The Chinese postman problem with load-dependent costs. Transportation Science, 52(2), 370-385.
- Dudzik, A., & Veličković, P. (2022). Graph neural networks are dynamic programmers. Advances in Neural Information Processing Systems, 35, 20635-20647.
- Hy, T. S., & Tran, C. D. (2024). Graph Attention-based Deep Reinforcement Learning for solving the Chinese Postman Problem with Load-dependent costs. arXiv preprint arXiv:2310.15516.
- Wang, Y., Jia, Y. H., Chen, W. N., & Mei, Y. (2025). Distance-Aware Attention Reshaping for Enhancing Generalization of Neural Solvers. IEEE Transactions on Neural Networks and Learning Systems.
- Zheng, Z., Zhou, C., Xialiang, T., Yuan, M., & Wang, Z. (2024). UDC: A Unified Neural Divide-and-Conquer Framework for Large-Scale Combinatorial Optimization Problems. Advances in Neural Information Processing Systems, 37.
- Berto, F., Hua, C., Cao, Z., Kwon, C., & Park, J. (2025). RRNCO: Towards Real-World Routing with Neural Combinatorial Optimization. arXiv preprint arXiv:2503.16159.
- Gundawar, A., Valmeekam, K., Verma, M., & Kambhampati, S. (2024). Robust Planning with Compound LLM Architectures: An LLM-Modulo Approach. arXiv preprint arXiv:2411.14484.
- Meng, S., Wang, Y., Yang, C. F., Peng, N., & Chang, K. W. (2024). LLM-A*: Large Language Model Enhanced Incremental Heuristic Search on Path Planning. Findings of the Association for Computational Linguistics: EMNLP.
- Li, K., Liu, F., Wang, Z., Tong, X., & Yuan, M. (2025). ARS: Automatic Routing Solver with Large Language Models. arXiv preprint arXiv:2502.15359.
- Liu, X., Hu, J., Li, Y., Diao, S., Liang, Y., Hooi, B., & Zimmermann, R. (2024). UniTime: A language-empowered unified model for cross-domain time series forecasting. Proceedings of the ACM Web Conference.
- Huang, Q., Zhou, Z., Yang, K., Lin, G., Yi, Z., & Wang, Y. (2024). LeRet: Language-empowered retentive network for time series forecasting. Proceedings of the 33rd International Joint Conference on Artificial Intelligence.